<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Simple CSS Example</title>

    <!-- Internal CSS -->
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
        }

        h1 {
            color: blue;
            text-align: center;
        }

        p {
            color: #555;
            font-size: 16px;
        }

        .flex-container {
            display: flex;
            justify-content: space-between;
            background-color: lightgray;
            padding: 10px;
        }

        .flex-item {
            background-color: lightblue;
            padding: 20px;
            text-align: center;
            flex: 1;
            margin: 5px;
        }

        .box {
            width: 100px;
            height: 100px;
            background-color: coral;
            margin: 10px;
        }
    </style>

    <!-- External CSS (via CDN) for Bootstrap -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
</head>

<body>

    <!-- Inline CSS Example -->
    <h1 style="color: green; text-align: center;">Welcome to My Webpage</h1>

    <!-- Internal CSS Example -->
    <p>This is a simple page styled with internal CSS.</p>

    <!-- External CSS (via Bootstrap) -->
    <div class="container">
        <h2 class="text-primary">Bootstrap Example Heading</h2>
        <p class="text-muted">This paragraph is styled using Bootstrap's CSS framework.</p>
    </div>

    <!-- Flexbox Example -->
    <div class="flex-container">
        <div class="flex-item">Item 1</div>
        <div class="flex-item">Item 2</div>
        <div class="flex-item">Item 3</div>
    </div>

    <!-- Basic Box Styling -->
    <div class="box"></div>

</body>

</html>
